app = "kompli-bot"

[build]
  image = "python:3.11-slim"

[env]
  # Не пушим токен в git! Используем fly secrets
  # BOT_TOKEN = "YOUR_BOT_TOKEN_HERE"
  # PUBLIC_URL = "https://kompli-bot.fly.dev"

[[services]]
  internal_port = 8080
  protocol = "tcp"

  [[services.ports]]
    port = 80

  [[services.ports]]
    port = 443
