[build]
  image = "python:3.11-slim"

[[services]]
  internal_port = 8080
  protocol = "tcp"
  [[services.ports]]
    port = 80
  [[services.ports]]
    port = 443

[deploy]
  release_command = "uvicorn main:app --host 0.0.0.0 --port 8080"
